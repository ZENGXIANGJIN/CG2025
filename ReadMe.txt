Exampe 09, also available on WeBeep, can be used as a starting point for loading external assets. You should be able to read these .mgcg files, just by setting the last parameter
to MGCG, i.e.:

		Msun.init(this, &VDEmission, "models/Sphere.mgcg", MGCG);
		
